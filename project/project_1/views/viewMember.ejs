<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원 상세</title>
    <link rel="stylesheet" href="/CSS/style.css">
</head>
<body>
<div class="container">
    <h2>회원 상세정보</h2>
    <table class="detail-table">
        <tr>
            <th>이름:</th>
            <td><%= member.name %></td>
        </tr>
        <tr>
            <th>성별:</th>
            <td><%= member.gender %></td>
        </tr>
        <tr>
            <th>전화번호:</th>
            <td><%= member.Pnumber %></td>
        </tr>
        <tr>
            <th>생년월일:</th>
            <td><%= member.ssn %></td>
        </tr>
        <tr>
            <th>배정 가능한 지역:</th>
            <td><%= member.regions %></td>
        </tr>
        <tr>
            <th>보험명:</th>
            <td><%= member.insurance_name || '미입력' %></td>
        </tr>
        <tr>
            <th>가입일자:</th>
            <td><%= member.join_date || '미입력' %></td>
        </tr>
    </table>

    <a href="/members/edit/<%= member.id %>" class="btn btn-primary">회원 수정</a>

    <form action="/members/delete/<%= member.id %>" method="POST" onsubmit="return confirm('정말 삭제하시겠습니까?');">
        <button type="submit" class="btn btn-danger">회원 삭제</button>
    </form>

    <div class="top-actions">
        <a href="/members" class="btn btn-secondary">목록으로</a>
    </div>
</div>
</body>
</html>